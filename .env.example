# MCP Servers Configuration
# Copy this file to .env and update with your actual values

# AWS Configuration (used by both MCP servers)
AWS_PROFILE=default
AWS_REGION=us-east-1

# S3 Bucket for storing assets and results
AWS_BUCKET_NAME=bedrock-data-automation-723648924038-us-east-1

# Base directory for local file operations (optional)
BASE_DIR=/home/robww/accelerated-intelligent-document-processing-on-aws

# Logging level (ERROR, WARNING, INFO, DEBUG) - used by all MCP servers
FASTMCP_LOG_LEVEL=ERROR

# AWS Pricing MCP Server
# No additional configuration needed - uses AWS_PROFILE and AWS_REGION above